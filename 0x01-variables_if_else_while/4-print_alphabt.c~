#include <stdio.h>
/**
 * main - Print alphabets except q and e
 *
 * Return: Always (0)
 */
int main(void)
{
char alpha[26] = "abcdefghijklmnopqrstuvwxyz";
int i;
for (i = 0; i < 26; i++)
{
if (alpha[i] == 'q' || alpha[i] == 'e')
{
continue;
}
putchar(alpha[i]);
}
putchar(alpha'\n');
return (0);
} 
