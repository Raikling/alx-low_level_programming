#include "main.h"
/**
 * _isupper - checks for uppercase character
 * Return: 1 if it's uppercase, or 0 otherwise
 * @c: character to be checked
 */
int _isupper(int c)
{ 
if (c >= 65 && c <= 90)
{
return (1);
}
return (0);
}
         
